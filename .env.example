# The app's own state store (branches, commits, users). Required.
APP_DATABASE_URL=postgres://localhost:5432/schemavc_dev

# Optional: a real database for the "Deploy to target" button.
# Without it, deploys show the SQL and offer "Mark as deployed".
TARGET_DATABASE_URL=

# Required in production; any long random string.
JWT_SECRET=change-me
